# AGENTS.md

本文件是 Codex 后续开发本项目时必须遵守的项目规则。

## 1. 项目定位

本项目是一个 Web/H5 版 AI 服装试衣 MVP 平台。

第一阶段目标：

用户上传服装正面图，上传人物照片或选择内置 AI 模特，选择服装类型和图片比例，生成试穿效果图。

当前阶段重点是验证最小闭环：

- 图片上传；
- 试衣任务创建；
- Mock AI 生成；
- 任务详情查看；
- 历史记录；
- 后台任务查看。

## 2. 技术栈

- Next.js App Router
- TypeScript
- Tailwind CSS
- Prisma
- PostgreSQL
- 当前 AI 服务是 mock，后续替换真实 AI API
- 当前图片存储可先本地，后续替换云存储

## 3. 开发原则

- 不要一次性重构整个项目。
- 每次只围绕用户提出的功能修改相关文件。
- 新增功能必须保持现有页面可运行。
- 修改数据库前必须同步更新 Prisma schema、迁移说明和 README。
- API 返回格式要统一。
- 不要删除已有功能。
- 不要引入不必要的大型依赖。
- UI 保持简洁、高级，适合服装电商和 AI 工具平台。
- 优先复用现有目录结构、组件和类型定义。
- 修改 AI 生成逻辑时，优先改 `lib/ai/tryonService.ts`。
- 修改数据库访问时，统一通过 `lib/db.ts` 中的 Prisma Client。
- 本地图片上传目录保持为 `public/uploads`。
- Mock 结果图保持在 `public/mock`。
- 页面需要处理加载、错误和空状态。
- 移动端优先，同时保证 PC 端布局美观。

## 4. 当前暂不开发

- 不做 3D。
- 不做视频。
- 不做背面和侧面。
- 不做真实尺码推荐。
- 不做复杂社交功能。
- 不做移动原生 App。

## 5. 每次完成任务后必须输出

每次完成开发或修复后，必须向用户说明：

- 修改了哪些文件；
- 如何运行和测试；
- 是否涉及数据库迁移；
- 是否还有已知问题。

输出应简洁清晰，优先给出用户下一步可以直接执行的命令。
